context('Actions', () => {
    beforeEach(() => {
        cy.visit("https://sbi.bnpl-demo.com/")

})

describe('loginTest',  () => {
    it('Visits the admin link', () => {
        cy.visit("https://sbi.bnpl-demo.com/")
     })
})

})